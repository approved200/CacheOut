# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.xcarchive

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Logs (debug only — never commit)
Logs/

# Sparkle private key — NEVER commit
*.pem
*.p8
sparkle_private_key*
edkey_private*

# Xcode SPM caches
.swiftpm/
Package.resolved

# CocoaPods (if ever added)
Pods/
Podfile.lock

# Build outputs
*.pkg
export/
build/

# Generated python cache
__pycache__/
*.pyc

# Mocked screenshots — may contain generated PNGs
screenshots/mocked/

#Claude
.claude

# Local release configuration — fill in YOUR_TEAM_ID before archiving.
# The template (exportOptions.plist) stays in git; local overrides go here.
exportOptions.local.plist
build/
